import React from 'react';

const Dashboard = () => {
    return (
        <div style={styles.container}>
            <h1>Temporary Dashboard</h1>
        </div>
    );
};

const styles = {
    container: {
        padding: '20px',
    },
};

export default Dashboard;